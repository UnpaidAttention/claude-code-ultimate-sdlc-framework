---
description:  Execute parallel planning for independent features
---

# /sdlc-planning-parallel

This command invokes the `sdlc-planning-parallel` workflow skill. Use the Skill tool to load the full workflow:

```
Skill: sdlc-planning-parallel
```
