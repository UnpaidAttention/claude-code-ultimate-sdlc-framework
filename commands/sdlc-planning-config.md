---
description:  Read and validate framework configuration
---

# /sdlc-planning-config

This command invokes the `sdlc-planning-config` workflow skill. Use the Skill tool to load the full workflow:

```
Skill: sdlc-planning-config
```
