---
description:  Phase 5 of UI Polish — implement the approved remediation plan in order (typography, colors, components, layouts, copy), verifying build after each category.
user_invocable: false
---

# /sdlc-dev-ui-polish-apply

This command invokes the `sdlc-dev-ui-polish-apply` workflow skill. Use the Skill tool to load the full workflow:

```
Skill: sdlc-dev-ui-polish-apply
```
