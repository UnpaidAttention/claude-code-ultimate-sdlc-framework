---
description:  Read and validate framework configuration
user_invocable: false
---

# /sdlc-dev-config

This command invokes the `sdlc-dev-config` workflow skill. Use the Skill tool to load the full workflow:

```
Skill: sdlc-dev-config
```
