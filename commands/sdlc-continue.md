---
description:  Auto-advance through framework phases. Reads state, determines next step, executes it. Pauses at gates and council transitions.
user_invocable: true
---

# /sdlc-continue

This command invokes the `sdlc-continue` workflow skill. Use the Skill tool to load the full workflow:

```
Skill: sdlc-continue
```
