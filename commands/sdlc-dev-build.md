---
description:  Verify build succeeds with evidence artifact
---

# /sdlc-dev-build

This command invokes the `sdlc-dev-build` workflow skill. Use the Skill tool to load the full workflow:

```
Skill: sdlc-dev-build
```
