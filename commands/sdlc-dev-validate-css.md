---
description:  Verify all CSS variables are defined and synchronized
---

# /sdlc-dev-validate-css

This command invokes the `sdlc-dev-validate-css` workflow skill. Use the Skill tool to load the full workflow:

```
Skill: sdlc-dev-validate-css
```
