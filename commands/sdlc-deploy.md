---
description:  Deployment command for production releases. Pre-flight checks and deployment execution.
---

# /sdlc-deploy

This command invokes the `sdlc-deploy` workflow skill. Use the Skill tool to load the full workflow:

```
Skill: sdlc-deploy
```
