---
description:  Execute parallel AIOU development with safety checks
---

# /sdlc-dev-parallel

This command invokes the `sdlc-dev-parallel` workflow skill. Use the Skill tool to load the full workflow:

```
Skill: sdlc-dev-parallel
```
