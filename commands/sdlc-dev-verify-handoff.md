---
description:  Validate planning handoff document before development
---

# /sdlc-dev-verify-handoff

This command invokes the `sdlc-dev-verify-handoff` workflow skill. Use the Skill tool to load the full workflow:

```
Skill: sdlc-dev-verify-handoff
```
