---
description:  Verify configuration and state changes persist across app restart
user_invocable: false
---

# /sdlc-validate-persistence

This command invokes the `sdlc-validate-persistence` workflow skill. Use the Skill tool to load the full workflow:

```
Skill: sdlc-validate-persistence
```
