---
description:  Non-destructive audit of existing UI. Orchestrates 6 phases to identify missing routes, unwired components, incomplete interactions — then plans and builds only what's missing. Does NOT delete existi
user_invocable: true
---

# /sdlc-dev-ui-audit

This command invokes the `sdlc-dev-ui-audit` workflow skill. Use the Skill tool to load the full workflow:

```
Skill: sdlc-dev-ui-audit
```
