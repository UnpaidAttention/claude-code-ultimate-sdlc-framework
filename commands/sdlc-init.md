---
description:  Initialize a new project with the Ultimate SDLC Framework. Creates all state files and guides to Planning Council.
user_invocable: true
---

# /sdlc-init

This command invokes the `sdlc-init` workflow skill. Use the Skill tool to load the full workflow:

```
Skill: sdlc-init
```
