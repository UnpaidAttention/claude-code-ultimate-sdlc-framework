---
description:  Run tests and capture results with evidence artifact
---

# /sdlc-dev-test

This command invokes the `sdlc-dev-test` workflow skill. Use the Skill tool to load the full workflow:

```
Skill: sdlc-dev-test
```
